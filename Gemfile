# 本地預覽用（GitHub Pages 線上建置不讀這個檔）：
#   docker run --rm -v "$PWD":/srv/jekyll -w /srv/jekyll ruby:3.3 \
#     sh -c "bundle install && bundle exec jekyll build"
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
