# MuJoCo 繁體中文教學專案 — 成果報告

> 更新日期：2026-09-10
>
> Repo：<https://github.com/wicanr2/mujoco-tutorial-zh>
>
> 成果摘要與影片見 [README.md](README.md)；本檔放完整數據、除錯案例與可行性結論。

## 一、專案目標

依 [AGENTS.md](AGENTS.md) 規範建立 MuJoCo 物理引擎的繁體中文教學，涵蓋官方文件收集與翻譯、
MJCF 建模、程式設計、URDF 匯入、Isaac Sim / Gazebo 整合可行性、RL 訓練範例、AMR 搬運實驗。
所有範例須實際執行驗證，資料須登記出處。

## 二、產出總覽

27 篇教學、35 支 Python 腳本、6 組錄影實驗、34 筆資料出處登記。

### 基礎系列（docs/00–05）

| 篇 | 文件 | 驗證狀態 |
| --- | --- | --- |
| 01 | MuJoCo 導論與安裝 | 本機實測 |
| 02 | MJCF 建模基礎（雙擺） | 本機實測 |
| 03 | 程式設計入門（PD 控制） | 本機實測 |
| 04 | URDF 匯入（二連桿 + 轉存 MJCF） | 本機實測 |
| 05 | Isaac Sim 整合（MJCF/URDF Importer 流程） | 文件整理，無 Isaac Sim 環境（文中已標註） |
| 06 | Gazebo 整合（gz-physics 外掛現況分析） | 文件整理（文中已標註） |

### 程式設計 / RL 系列（docs/02-programming）

| 篇 | 文件 | 驗證狀態 |
| --- | --- | --- |
| 07 | 五個 Python 範例（感測器/伺服/keyframe/平行取樣/mjSpec） | 全部實測 |
| 08 | RL：ARS 手寫實作 | 21 秒收斂至 -0.30 |
| 09 | RL：Gymnasium + PPO | 6 分鐘收斂至 -0.11 |
| 10 | 車桿 swing-up（能量整形 + mjd_transitionFD + LQR） | 1.19 秒盪起後穩定 |
| 11 | Viewer 與離屏渲染（osmesa） | 實測 + 渲染圖 |
| 12 | RL：SAC 對照實驗 | CPU 未收斂（如實記錄）→ GPU 收斂至 -0.21 |

### AMR 系列（docs/06-amr）

| 篇 | 文件 | 驗證狀態 |
| --- | --- | --- |
| 13 | AMR 叉車建模與牙叉控制（取貨流程） | 棧板抬到 z=0.14 m |
| 14 | 搬運車機械手臂 + 阻尼最小平方法 IK | 末端誤差 2.6 cm |
| 15 | 六軸手臂 6D IK（位置 + 姿態） | 30 次迭代，1.1 cm / 0.036 rad |
| 16 | 棧板材質實驗（木頭 vs 塑膠，20 kg） | 木 0.4 cm vs 塑膠 11.9 cm 滑動 |
| 17 | 門架前傾滑落邊界掃參 | 三案例實測 + 理論對照 |
| 18 | MR1533 真實 mesh 叉車（TB3 資產匯入） | 升降 0.951 m、前傾 9.5°，Blender 渲染 |
| 19 | Blender 程序化棧板建模（STL）+ 載重驗證 | 兩材質通過上下左右測試 |
| 20 | 真實模型重跑 16/17 章 | 材質皆 15.7 cm；前傾跟隨牙叉至著地 |
| 21 | MR1533 + y 向 reach 滑台（Blender 建模） | reach ±0.45 m，伸出狀態可升降 |
| 22 | y-reach 取放任務（錄影 + 6DOF 軌跡） | 19 秒完成，571 幀、951 筆記錄 |
| 23 | 連續動作：升降 ×3 + 側移 ×3 | 831 幀，漂移 22 cm → 1.4 cm |
| 24 | 舵輪底盤 + 環氧地板 + 貨架取放（reach X/Z） | 搬運 2.07 m、漂移峰值 6.8 cm、落地 z=0 |
| 25 | reach X/Y/Z 全軸取放 | 搬運 2.05 m、漂移峰值 12.1 cm、側移對位 0.49 m |
| 26 | 搬運車夾爪 A 取 B 放（weld 抓取） | 箱子最終落在 B 桌面 |
| 27 | 舵輪繞圈 waypoint 追蹤 | 8 點到達 7 點，4500 幀 |

附錄：`docs/glossary.md` 中英術語對照表、`sources/SOURCES.md` 資料出處登記（34 筆）。

## 三、RL 對照實驗數據

同一單擺 swing-up 任務（觀測 `[cosθ, sinθ, ω/8]`，動作 `u ∈ [-1, 1]`）：

| 方法 | 最終回報 | 訓練時間 | 樣本數 | 硬體 |
| --- | --- | --- | --- | --- |
| ARS（手寫 numpy） | -0.30 | 21 秒 | 30 輪 | CPU |
| PPO（SB3） | -0.11 | 6 分鐘 | 150k 步 | CPU |
| SAC（`train_freq=4`、`lr=3e-4`） | 未收斂（≈ -2.0） | 30 分鐘 | 60k 步 | CPU |
| SAC（zoo 超參：`train_freq=1`、`lr=1e-3`、8 環境） | -0.21 | 4 分鐘 | 60k 步 | RTX Pro 6000 |

SAC 的樣本效率需要足夠的更新頻率。CPU 上為了跑得動而降低 `train_freq`，60k 步內回報幾乎
不動；GPU 補回更新頻率後同樣 60k 步就收斂，用的樣本不到 PPO 的一半。同一個環境 PPO 與 ARS
都收斂，可以排除環境與回報函數的問題。遠端 GPU 訓練環境已於實驗後清除，權重保留在
`policies/swingup_sac_gpu.zip`。

## 四、AMR 實驗數據

**取貨流程（13）**：對位 → 插入 → 升降 → 側移，棧板抬到 z=0.14 m。

**手臂 IK（14/15）**：3D 位置 IK 末端誤差 2.6 cm；6D IK 30 次迭代收斂，位置 1.1 cm、
姿態 0.036 rad。

**摩擦對照（16）**：急側移下木頭棧板（μ=0.6）相對滑動 0.4 cm，塑膠（μ=0.35）11.9 cm，
兩者都沒掉落，但塑膠已接近牙叉邊緣。

**前傾邊界（17）**：以「相對位移超過 3 cm」為滑動判準，木頭 25.9°、塑膠 26.1°、塑膠加
重心前移 26.6°；45° 內三者都沒掉落。塑膠在 20.6° 就有可量到的蠕動，接近理論值
atan(0.35)=19.3°；木頭的 25.9° 反而低於理論 31.0° — 門檻量到的是 stick-slip 累積蠕動，
不是理論上的瞬間滑動角。快速前傾（4 秒）反而都不滑，準靜態才是最不利情況。

**平台對照（20）**：同樣的材質實驗搬到 2000 kg 的 MR1533 上，木頭與塑膠的滑動量都是
15.7 cm，到小數點後一位都相同。量測 `data.contact[i].friction` 確認摩擦係數確實生效；
力度掃描顯示加速度 4 m/s² 以下時兩者都只有接觸彈性變形，更大加速度時兩者都進入飽和滑動。
MR1533 沒有側移機構，橫移只能靠底盤 vy，加速度不是太小（都不滑）就是太大（都飽和），
做不出「摩擦剛好不夠」的中間地帶，材質差異落在觀測不到的區間。

**前傾卸貨（20）**：MR1533 的棧板重心靠近叉尖，前傾時重力力矩把棧板壓在叉面上，法向力增加，
棧板俯仰角完全跟隨牙叉直到前端著地（最終 z=0.142）— 這正是真實叉車的卸貨方式。

**連續動作漂移（23）**：升降循環中棧板每次落底往叉尖蠕動，累積 22 cm；門架後傾 0.08 rad
讓貨靠上滑架背板後降到 1.4 cm。

**貨架取放（24/25）**：舵輪底盤在環氧地板（μ=0.7）上原地取貨成立 — stage 深插 -0.65 m、
微升 4 cm 離開層板、升到 0.70 m 搬運高度，棧板確實被抬離層板（z=0.709）。加上 y 向 reach
後三軸都有作用，但側移 0.40 m 時棧板只跟隨約 0.09 m，摩擦傳遞在加速段會滑動，與 16/20 章一致。

**這兩章的搬運與放置段原本不成立，第四輪修好了**。原本的症狀：24 章車子前進 0.758 m 期間
棧板只移動 0.206 m，貨從叉齒滑落在地；25 章「放低」後棧板仍在叉齒上，接著 `drive_to` 的
`hold` 把門架重新升起（棧板衝到 z=1.251）再掉落。兩章的判定都只看最終位置，把「掉下去」
算成「放下去」。

修好之後：24 章搬運 2.07 m、搬運全程的漂移**峰值 6.8 cm**（終點收斂到 1.0 cm）、最終落到
z=0；25 章搬運 2.05 m、漂移峰值 12.1 cm、側移對位 0.49 m、落地 z=0。三個關鍵修正見第七節
第四輪。

**夾爪抓取（26）**：weld equality constraint 抓取，箱子抬到 z=0.616、最終落在 B 桌面
(0.91, -0.495, 0.43)。

**舵輪繞圈（27）**：2×2 m 正方形 8 個 waypoint，到達 7 個，轉彎外擺 0.41 m。舵輪不能原地
轉，路徑必然外擺。第 8 個點沒到：切換目標後車子以最大轉向角（1.39 rad）在 (0.3, 1.7)
附近繞小圈，94 秒都沒收斂進 0.35 m 容差 — 轉彎半徑大於容差圈就會繞著目標打轉。

## 五、除錯案例清單

每一條在對應章節都有現象、原因與解法的完整記錄。

**建模與接觸**

1. 機構內部零件互相碰撞造成摩擦鎖死（裝飾輪、滑架與門架、叉齒與底盤，共出現三次）— 關掉內部碰撞或用 `contact/exclude`
2. MuJoCo 接觸摩擦預設取兩個 geom 的最大值，一端設高就蓋掉另一端的材質
3. 棧板做成實心箱子，牙叉永遠插不進去 — 要照真實棧板留腳與空隙
4. MuJoCo 編譯器對 mesh 做主軸重新對齊，對稱 mesh 方向會亂 — 用 STL 並 join 成單一 mesh
5. URDF 不做 schema 檢查，拼錯的屬性會被靜默忽略
6. 模型沒有 `<light>` 時畫面極暗 — MuJoCo 只有一盞預設頭燈，不做全域光照

**控制與量測**

7. `<position>` 伺服在重力方向有穩態誤差，`kp` 要夠大或加前饋；`kp` 調高後 20 kg 載重會 overshoot，得補 `kv`
8. 幾何高度靠心算會錯（叉尖 z 差了 8 cm）— 用 `site_xpos` 實際量測
9. 共用控制函式的預設值會把其他通道歸零（`drive_to` 把 lift 歸零，出現三次）
10. 掉落判定不能用絕對高度，門架前傾本身就會降低棧板高度 — 改用姿態角或相對位移
11. 滑動量要扣掉機構本身的位移，且參考座標系要選對（tilt 會旋轉牙叉座標系）
12. 漂移量要以「取貨完成時」為基準，不能對零點判斷
13. freejoint 的 qpos 是 7 維含四元數、qvel 是 6 維，索引用 `model.body_jntadr` 最穩
14. 開環時間控制會衝過頭（對位差 0.13 m）— 改閉迴圈 P 控制

**IK 與抓取**

15. IK 不收斂先檢查目標在不在工作空間內，阻尼最小平方法會停在「全伸展」的局部最小值
16. 姿態誤差用官方的四元數差向量部分，`mju_subQuat` 的參考系與 `jacr` 不一致會發散
17. IK 解要檢查關節限位，超出 `ctrlrange` 會被夾住；但不要在迭代內硬 clip
18. weld 抓取啟動前要把當前相對位姿寫進 `eq_data`，否則物體會瞬間被吸到 anchor 點
19. 夾爪閉合行程等於物體寬度時夾持力為零 — 要留 over-travel

**求解器與訓練**

20. 輪地接觸加大轉向增益在 timestep 0.002 下發散（NaN QACC）— 降到 0.0005
21. `mjd_transitionFD` 回傳的是離散 Jacobian，接 LQR 前要除以 dt
22. ARS 這類隨機搜索對探索尺度敏感；力矩不足時先檢查致動器 `gear` 上限
23. SAC 在 CPU 上降低 `train_freq` 就學不動 — off-policy 需要足夠更新頻率
24. `renderer.render()` 不帶參數每次回傳新陣列；只有傳 `out=buf` 重用緩衝區時，收集到的影格才會全部是同一張
25. headless 環境的 matplotlib 預設字型沒有 CJK，圖表標題用英文

**實驗設計**

26. 判定條件只檢查終點位置時，「掉下去」會被算成「放下去」— 搬運與放置類實驗要檢查過程（z 單調下降、貨與車的相對位移），不是最後一幀
27. 模型與腳本裡的絕對路徑在作者的機器上永遠正常，別人 clone 下來直接失敗 — 要驗可攜性只能換一個工作目錄或換一台機器跑

## 六、Isaac Sim / Gazebo 可行性結論

**Isaac Sim** 綁定 PhysX，物理引擎不可替換。可行路徑是模型互通：MJCF/URDF Importer
（6.0 起改用 `mujoco-usd-converter`）把模型帶進 Isaac Sim，或以雙引擎工作流程各自負責
不同任務。

**Gazebo** 的 gz-physics 是外掛架構，可換引擎。官方 MuJoCo 外掛的初步實作已於 2026-03-14
併進主線（PR #811），之後持續補功能（最近一次 2026-09-09），但最新發行版 gz-physics9 9.0.0
（2025-10-14）早於合併日，要用得自己從主線建置，且 Feature 覆蓋還不完整。生產用途現階段
仍以 URDF 為共同交換格式，兩邊各自跑。（查證日期：2026-09-10）

兩章都沒有實機環境可驗證，文中已標明這一點。

## 七、稽核紀錄（2026-09-10）

同一天做了四輪，每一輪問不同的問題：第一輪「數字對不對」、第二輪「斷言對不對」、
第三輪「照著做跑得起來嗎、實驗真的證明了它宣稱的事嗎」、第四輪「把第三輪標出來的問題
實際修好」。**後一輪都抓到前一輪漏掉的東西**，連第三輪的診斷本身都在第四輪被修正了一半 ——
這本身就是一個結論：單一角度的檢查不會收斂。

### 第一輪：重跑比對

用 `scripts/verify_examples.sh` 把 35 支腳本中的 29 支逐支重跑一遍，確認文件裡的數字仍然
成立，**全部 exit code 0**；三支 Blender 腳本另外用 Blender 4.2.11 跑過，也都通過。沒跑的
2 支是 `ex_rl_sac.py`（CPU 上要 30 分鐘且已知不收斂，結論已記在 12 章）與
`ex_rl_sac_gpu.py`（需要 CUDA）。

| 批次 | 支數 | 耗時 | 結果 |
| --- | --- | --- | --- |
| 基礎與 AMR 非渲染（`basic`） | 17 | 每支 0.2–1.2 秒 | 全數通過 |
| 渲染與錄影（`render`，`MUJOCO_GL=osmesa`） | 10 | 0.7–1118 秒 | 全數通過 |
| RL 訓練（`rl`） | 2 | ARS 19 秒、PPO 1788 秒 | 全數通過 |
| Blender 建模與渲染 | 3 | 各數十秒 | 全數通過 |

`runs/` 的影片與 CSV 重跑後與版控中的檔案位元完全相同，Blender 匯出的 STL 也是 — 模擬與
程序化建模都是確定性的。PPO 有固定 `seed=0`，重跑後平均回報同為 -0.1065；這次耗時
1788 秒是因為同時跑渲染批次搶 CPU，單獨跑約 6 分鐘。

例外是 Blender 的 EEVEE 渲染圖：每次取樣結果略有不同，位元不一致但視覺內容相同。這類
圖重跑後直接還原，不要拿去覆蓋版控裡的檔案，否則每次驗證都會製造無意義的二進位 diff。

這輪比對抓到七處文件與實跑對不上，已全部更正：

| 位置 | 原本 | 實跑 |
| --- | --- | --- |
| 19 章 Blender 棧板滑動量 | 2.4 cm | 1.7 cm |
| 20 章真實模型材質滑動量 | 1.3 cm | 15.7 cm（兩材質都飽和滑動） |
| 26 章位置伺服穩態誤差 | 4.5 cm | 4.8 cm |
| 27 章轉彎外擺 | 約 0.3 m | 0.41 m |
| 22 章棧板 z 峰值與 y 極值 | 0.53 / -1.05 | 0.54 / -1.07 |
| 19 章測試環境版本 | MuJoCo 3.12.3 | 3.12.0（筆誤） |
| 24/25 章動作序列參數 | 依腳本 print 字串 | 依實際 `ctrl` 值（print 已同步修正） |

另外修掉兩個程式問題：`ex_viewer.py` 的四張輸出影格原本完全相同（桿放在 `qpos[1] = π`
這個穩定平衡點又沒有控制輸入，畫面靜止），改成從偏離 0.6 rad 出發；`ex_rl_sac_gpu.py`
的模型路徑原本是扁平的 `pendulum_swingup.xml`，在 repo 根目錄跑不起來，改成 repo 相對路徑。

### 第二輪：斷言稽核（同日）

第一輪比對的是「數字對不對」，這一輪把文件裡所有**可驗證的斷言**逐項對回實際檔案：模型參數
（質量、關節範圍、摩擦、timestep）、幾何量測（mesh 尺寸、面數、手臂可達範圍）、程式碼片段
與腳本本體、MuJoCo API 行為、以及外部連結與事實。

先講對的部分，這些寫實測驗證都成立：接觸摩擦取兩 geom 最大值、`mjd_transitionFD` 回傳離散
Jacobian（對角 ≈1）、四元數 `(w,x,y,z)` 慣例、編譯器對 mesh 做主軸對齊（實測 `mesh_quat`
= [0, .707, 0, .707]）、URDF 不做 schema 檢查、Renderer 預設 framebuffer 640×480、GIL 讓
多執行緒 rollout 不加速（實測 0.33×）、三軸手臂全伸 0.62 m、棧板 1.0×0.8 m、chassis mesh
74566 面、LQR 增益、PPO 與 SAC 的每一個超參。41 個外部連結有 40 個仍有效。

抓到的問題有六處：

| 位置 | 原本的斷言 | 查證結果 |
| --- | --- | --- |
| 13 章 | 「56 kg 車重」 | 模型算出來底盤 80 kg、車體合計 83.4 kg，沒有一項是 56 |
| 22 章除錯 5 | 「`render()` 回傳內部陣列，不 `.copy()` 所有影格都是同一張」 | 3.12.0 的 `render()` 不帶 `out=` 時每次配置新陣列（原始碼與實測都是）。只有傳 `out=buf` 才會共用 |
| 11 章 | 「沒燈就是黑的」 | MuJoCo 有一盞預設頭燈。拿掉 `<light>` 後平均亮度 24.5 → 15.4（255 階），是極暗不是全黑 |
| 06 章 | 「MuJoCo 外掛只有未維護的 MVP」 | 官方外掛的初步實作已於 **2026-03-14 併進 gz-physics 主線**（PR #811，21 檔 +2158 行），之後持續開發，最近一次 commit 2026-09-09 |
| SOURCES | `ignitionrobotics/ign-physics#299` | 組織已改名，該網址 404；正確位置是 `gazebosim/gz-physics#299` |
| 05 章 | 提到 `mujoco-usd-converter` 但沒給位置 | 實際在 `newton-physics/mujoco-usd-converter`（Apache-2.0、PyPI 0.5.0、官方自述 Alpha），已補連結與現況 |

前三項是本地可驗證卻沒驗的，後三項是**外部事實過期**——寫的時候是對的，三個月後就不是了。
外部事實現在都標了查證日期。

順帶補強兩處論述：15 章的 `mju_subQuat` 加上收斂對照表（四元數差寫法在步長 0.2/0.4/0.8
分別 76/37/18 次收斂，`mju_subQuat` 三種步長跑滿 200 次都卡在誤差 ≈ π）；04 章的 URDF
schema 加上對照（同一個拼錯在 MJCF 報 `Schema violation`、在 URDF 靜默通過）。

**教訓一**：外部事實會過期，而且不會通知你。凡是引用別人專案狀態的句子都要標查證日期，
定期重查——尤其「某功能還不存在」這種否定斷言，它最容易在你沒看的時候變成假的。

**教訓二**：錯誤斷言會擴散。22 章那條「共用 buffer」在第一輪不但沒被抓到，我還照著它在
11 章寫了一段新的說明，等於把錯誤複製了一份。引用另一份文件的說法之前，要先驗那份說法。

**教訓三**：腳本的 `print` 字串會過期，它跟實際的 `ctrl` 值是兩份資料。24/25 章的動作參數
一度依 print 改錯了方向，回頭讀 `ctrl` 向量才確認原文件是對的。判斷實驗做了什麼，要看
送進模擬的數值，不是看印出來的字。

### 第三輪：教學可用性稽核（同日）

前兩輪確認「數字對不對」與「斷言對不對」。這一輪站在讀者的位置問兩件事：**照著做跑得起來
嗎？實驗真的證明了它宣稱的事嗎？**

**核對官方文件**：00-intro 的每一條與官方 Overview 逐字吻合（Roboti LLC → 2021-10 DeepMind
收購 → 2022-05 開源、執行期零記憶體配置、Newton 求解器二次收斂、MJX 與 MuJoCo Warp、模型
層級表）。02-programming 的 AVX 需求、六個模組的語言、型別與函式前綴表、「函式前綴必以底線
結尾」也全部吻合。01-basics 的四條實測通過：MJCF 預設 `angle="degree"`、無 joint 的 body
被焊死、四種 joint 型別、方向屬性互斥（官方報 `multiple orientation specifiers are not
allowed`）。04 章的「URDF 預設 radian」也實測成立 — 同一個 `1.5708` 在 URDF 當弧度、在 MJCF
當度數。`mjData` 欄位維度表逐欄比對無誤。

**跑得起來嗎**：發現四處硬編的絕對路徑 `/home/anr2/tmp2/mujoco/...`（`models/mr1533_steer.xml`
的 `meshdir` 與 yreach mesh、`ex_reach_xz.py`、`ex_reach_xyz.py`）。這在作者自己的機器上永遠
不會出錯，但**任何人 clone 下來，24、25、27 三章都會直接失敗**。已改成相對路徑（模型）與
執行期由 `__file__` 算出的路徑（腳本），並實測從 `/tmp` 載入成功。

**實驗真的證明了它宣稱的事嗎**：這是這輪最重要的發現，24 與 25 章的結論不成立。

| 章 | 稽核前腳本印出 | CSV 顯示的實際情況 |
| --- | --- | --- |
| 24 | 「原地取放（reach X/Z）驗證通過 ✓」 | 取貨成立（棧板抬到 z=0.710）。但前進段車體移動 0.758 m、棧板只移動 0.206 m，**相對滑動 0.55 m**，貨從叉齒滑落在地。叉齒水平沒有後傾，慣性把貨往叉尖推 — 正是 23 章量過、也解決過的現象 |
| 25 | 「reach X/Y/Z 取放驗證通過 ✓」 | 三軸機構動作成立。但 `lift1=0.05` 的「放低」棧板仍在叉齒上（z=0.368），接著 `drive_to` 的 `hold=(0.70, 0.35)` 把門架重新升起（棧板衝到 **z=1.251**），貨在行進中掉落 |

共同的根因是**判定條件只檢查最終位置**：25 章的斷言是「棧板 z<0.05 且 x>-1.0 且 |y|>0.05」，
貨掉在地上剛好全部滿足，於是「掉下去」被算成「放下去」。

對照組：22 章與 26 章的放置是真的成功 — 22 章棧板從 0.391 平順降到 0，全程 pitch ≈ 0 沒有
翻滾；26 章箱子最終 z=0.430，減去半邊長 0.03 正好是桌面高度 0.40，最後三秒完全靜止。

兩章的文件已改寫成實際行為，斷言也改成只驗它真正證明的事（24 章驗「棧板被抬離層板」、
25 章驗「三軸機構都有動作」），搬運與放置段的問題明確標為未完成項。**沒有把實驗改到
「成功」** — 試著把 25 章的
`hold` 改成 `(0.05, 0.02)` 之後，棧板確實不再被舉起，但車子的航向控制在低速段失去穩定、
往反方向跑掉 6.5 m。要把放置段做對得連控制器一起重調，那是另一件工作，不該混在稽核裡假裝
順手修好。

**其他**：25 章補上 AGENTS.md 要求的「常見錯誤與除錯」段（原本缺）。文件中 3 個完整 MJCF
區塊都能載入；各章結構齊備、前置知識沒有指向後面的章節；文件提到的 pip 套件都在
`requirements.txt`；影片幀數與 CSV 筆數用 `ffprobe` 實數核對與文件一致。

**教訓四**：驗收條件寫什麼，就只驗到什麼。「抬起來過」與「搬到了」是兩件事，寫前者就永遠
抓不到後者的失敗。設計判定時要先問：我真正想證明的是哪一件事？過程檢查（z 是否單調下降）
往往比終點檢查更能分辨「放下」與「掉落」。

**教訓五**：絕對路徑在自己的機器上永遠測不出問題。要驗可攜性，唯一的方法是換一個工作目錄
或換一台機器跑跑看。

### 稽核之後：把檢查固化成 CI

三輪抓到的問題有一半屬於「在作者機器上永遠正常」那一類 —— 絕對路徑、清單沒同步、宣稱的
數字與實際對不上。這些靠人工稽核會漏，靠 CI 不會。

`scripts/check_docs.py` 把十項檢查寫成程式：絕對路徑、文件相對連結、模型可載入、文件裡的
MJCF 區塊、章節結構、章節編號與依賴順序、各目錄 README 的清單登記、孤兒圖片、相依套件登記、
README/REPORT 宣稱的數量。`.github/workflows/verify.yml` 每次 push 與 PR 在**乾淨的
checkout** 上跑它，加上 17 支不需渲染的範例（runner 上 5 秒跑完），並檢查重跑後 `runs/`、
`models/`、`docs/assets/` 沒有變動。渲染批次因為要 40 分鐘，設成手動觸發。

這支腳本第一次跑就抓到三處不同步 — 包括它自己被加進 `scripts/` 之後造成的兩處數字落差。
**稽核要能重複執行才有意義**；只在對話裡跑一次的檢查，下次還是會漏。

### 第四輪：把 24 / 25 章修到真的成立

第三輪只把問題標為未完成項，沒有修。這一輪修好了 —— 而且過程中發現原本的診斷只對了一半。

**原本以為是摩擦不足**（貨在加速時往叉尖滑），實際上是三個獨立的問題疊在一起：

| 問題 | 症狀 | 修法 |
| --- | --- | --- |
| `stage` 深插被中途收回 | 「升到搬運高度」那一步把 `stage` 從 -0.65 設回 0，支撐點退到叉尖，roll 立刻掉到 **-63°**（翻了） | 深插後每個 `step` 與 `drive_to` 都傳 `stage=-0.65` |
| 在貨架內升到搬運高度 | 棧板頂面 0.842 m，貨架上層層板底面 0.85 m —— **只剩 8 mm**，擦撞翻覆 | 先保持低位退出貨架，離架後才升高 |
| 航向目標寫死成 π | 車體 -x 側就是牙叉、貨架也在 -x，**yaw=0 本來就是對的**。設 π 等於要求舵輪車原地轉 180°，而航向誤差又透過 `max(0.15, 1-\|herr\|)` 把速度壓到 15% | 目標改成 0，`herr` 全程 < 0.5° |

第三個問題的表現最有欺騙性：車子以 0.03 m/s 爬行、20 秒只轉 19°，看起來像「驅動力不足」
或「輪地摩擦太小」，實際上是控制目標設錯，動力完全正常。診斷時是把 `yaw`、`herr`、`drive`
一起印出來才看穿的 —— **只看結果（車子走不動）會往錯的方向查**。

修好之後（含後傾 0.08、線速度上限約 0.23 m/s）：

| | 修正前 | 修正後 |
| --- | --- | --- |
| 24 章搬運距離 | 貨掉在半路 | **2.07 m** |
| 24 章牙叉座標系漂移峰值 | 110 cm | **6.8 cm**（終點 1.0 cm） |
| 24 章最終棧板 z | 0.199（掉落殘骸） | **-0.000**（平穩落地） |
| 25 章 | 貨被重新舉起後掉落 | 搬運 2.05 m、漂移峰值 12.1 cm、側移對位 0.49 m、落地 |

兩章的斷言改成同時檢查四件事：抬離層板、牙叉座標系漂移在容差內、搬運距離、最終落到地面。
CSV 也加了 `rel_x/y/z`（棧板在牙叉座標系中的位置），漂移可以逐幀查證。

**漂移要取峰值不是終點值。** 第一版把終點漂移（24 章 1.0 cm）當結論寫進文件，回頭掃 CSV
才發現搬運過程中的峰值是 6.8 cm —— 貨在起步加速時滑出去，後傾又把它帶回背板。只看最後
一幀會低估過程中的風險，這正是這一輪自己剛寫下的教訓。腳本現在用 `track_drift()` 追蹤
整段峰值，斷言也對峰值設容差。

**教訓六**：一個症狀可能對應多個獨立成因。第三輪看到「貨滑落」就歸因於摩擦與加速度，
掃了四組加速度參數都沒改善 —— 因為真正的三個原因裡沒有一個是加速度。掃參數之前先確認
機制：把中間變數印出來，看它們是不是你以為的值。

**教訓七**：`max(0.15, 1-|herr|)` 這種「誤差大就減速」的保護項，會把控制目標設錯的後果
偽裝成動力問題。保護項讓系統不會失控，但也讓錯誤變得難以診斷 —— 檢查時要把它拿掉看看，
或至少把它的輸入印出來。

## 八、環境與版本

- 本機：Ubuntu 24.04、Python 3.12.3、MuJoCo 3.12.0、numpy 2.5.3、scipy 1.18.1、
  imageio 2.37.4、matplotlib 3.11.1、Blender 4.2.11 LTS（headless EEVEE）
- RL：Stable-Baselines3 2.9.0、Gymnasium 1.3.0、PyTorch 2.14.0
- GPU 訓練：RTX Pro 6000（torch cu130），環境已清除
- 版本鎖定在 `requirements.txt`

## 九、後續工作

1. 05 / 06 章在有 Isaac Sim 或 Gazebo 的機器上實測後更新。Gazebo 那邊官方 MuJoCo 外掛已在
   主線，可以從原始碼建置實測（見第六節）
2. 03 章補 Menagerie 真實機器人實例
3. AMR 線：差速底盤、滿載爬坡穩定性、用 RL 訓練對位策略
4. 外部事實（第六節的 Isaac Sim / Gazebo 現況）定期重查，目前查證日期是 2026-09-10
5. 渲染批次目前是手動觸發（一次約 40 分鐘）。要改成定期跑的話，排程比每次 push 合理
